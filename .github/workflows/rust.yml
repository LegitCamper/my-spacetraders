name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-and-test:
    runs-on: self-hosted
    steps:
      - name: deps
        run: sudo apt-get update && apt-get install -y build-essential pkg-config libssl-dev
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          
      - uses: actions-rs/cargo@v1
        with:
          command: test
          
      - uses: actions-rs/cargo@v1
        with:
          command: build
          args: --release --all-features
      
      - name: Rename
        run: mv target/release/interface target/release/my-spacetraders.bin
        
      - name: Upload
        uses: actions/upload-artifact@v3
        with:
          name: my-spacetraders
          path: target/release/my-spacetraders.bin

      - name: Docker
        id: build
        uses: cloudposse/github-action-docker-build-push@main
        with:
          registry: registry.sawyer.services
          platforms: linux/amd64,linux/arm64

    outputs:
      image: ${{ steps.build.outputs.image }}
      tag: ${{ steps.build.outputs.tag }}
